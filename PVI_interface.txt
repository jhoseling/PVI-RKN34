#include <stdio.h>
#include <stdlib.h>
#include <math.h>

         void fun_f(double const *t, double const *u) {
                                 switch (np){
                                             case 1 :
                                                  double res1=2,res2=8,L=1,Ca=0.5,E=200;
                                                  double *du,*t,*u;
                                                         du[0]= (E/L)- u[0]*(res1/l)+u[1]*(res1/l);
                                                         du[1]=(((E*(res1/L))-(u[0]*(pow(res1,2))/L)+ (u[1]*(((pow(res1,2)/L)-1/Ca))))*(1/(res1+res2);
                                                         du[2]= u[2];
                                             break;
                                  }
       
       
         }
       
         void PVI_interface (int const np) {
                                 printf("ingrese np:\n");
                                 switch (np){
                                             case 1 : 
         
                                                  double const *dom, *alpha;
                                                  int Neq = 3;
                                                  dom= {0,100};
                                                  alpha= {0,0,0};
                                                  PVI_RKN34(dom;alpha;Neq);
                                                  break;
                                  
                                  }
         system("PAUSE");
          return 0; 
         }
